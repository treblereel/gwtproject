package java.time.zone;

import java.time.LocalDateTime;
import java.time.ZoneOffset;

// JDK version, see for real code in /src/test/super
public class ZoneOffsetTransitions {

  private ZoneOffsetTransitions() {
    // prevent instantiation
  }

  public static ZoneOffsetTransition create(
      LocalDateTime transition, ZoneOffset offsetBefore, ZoneOffset offsetAfter) {
    return null;
  }
}
